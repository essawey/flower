
    # def plot_training_curve(self, results: Dict[str, List[float]]):
    #     """
    #     Plot training curves based on the results.
        
    #     Args:
    #         results: A dictionary of metrics tracked during training.
    #     """
    #     # Assuming matplotlib or similar library is used
    #     import matplotlib.pyplot as plt

    #     plt.figure(figsize=(12, 8))
    #     for key, values in results.items():
    #         plt.plot(values, label=key)
    #     plt.xlabel("Epochs")
    #     plt.ylabel("Metric Value")
    #     plt.title("Training Metrics Over Epochs")
    #     plt.legend()
    #     plt.grid(True)
    #     plt.show()
